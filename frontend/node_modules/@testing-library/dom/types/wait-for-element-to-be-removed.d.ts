import {waitForOptions} from '@testing-library/dom/types/wait-foribrary/dom/types/wait-for'

export function waitForElementToBeRemoved<T>(
  callback: T | (() => T),
  options?: waitForOptions,
): Promise<void>
